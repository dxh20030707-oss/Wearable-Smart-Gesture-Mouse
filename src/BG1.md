# 🚀 ESP32-C3 智能飞鼠/触控一体鼠标项目阶段性总结报告

> **版本 v3 ｜ 更新日期 2026-09-21 ｜ 对应固件：三模式架构（模式 1 / 2 / 3）**
>
> 本报告已按**当前实际代码**整体重写。上一版（v2）描述的「两模式 + EC11 正交编码器滚轮」架构已**作废**，主要变更：
> GPIO 0/1 由编码器改为**触摸按键 + 动力学平滑滚动**、新增**模式 3 手机 BLE 调参**与 **NVS 参数持久化**、模式切换由运行时热切换改为**落盘 + 重启**。
>
> 文档分工：`README.md` 面向外部展示，本报告面向内部复盘（架构事实 / 缺陷清单 / 演进规划）。
> 报告中的 `文件名:行号` 均对应当前版本，代码变动后请同步更新。

系统基于 **ESP32-C3** 主控，利用 PlatformIO + Arduino 框架开发。通过 GPIO 时分复用架构，将惯性传感器（MPU6050）、电容触控屏（FT6336U）、OLED 屏幕、正交编码器（EC11）与触摸按键有机结合，实现三种工作模式的切换。

---

## 📌 一、架构现状梳理

### 1. 三模式系统架构

| 模式 | 枚举值 | BLE 身份 | 主外设 | 进入方式 | 退出方式 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **模式 1 · 飞鼠** | `MODE_FLY_MOUSE = 0` | `ESP32-C3 SmartMouse`（标准 HID 鼠标） | MPU6050 + OLED | 开机恢复 / 短按 GPIO 10 | 短按 → 模式 2；长按 ≥1.5s → 模式 3 |
| **模式 2 · 电容触控** | `MODE_TOUCH_PAD = 1` | 同上（HID 鼠标） | FT6336U + EC11 + OLED | 短按 GPIO 10 | 短按 → 模式 1；长按 ≥1.5s → 模式 3 |
| **模式 3 · App 调参** | `MODE_APP_CONFIG = 2` | `SmartMouse-Config`（自定义 128-bit GATT 服务） | OLED（状态显示） | 长按 GPIO 10 ≥1.5s | 长按 ≥1.5s → 模式 1（**此模式下短按无效**，见 `main.cpp:148`） |

**关键机制**：模式状态由 `g_current_mode` 单一变量承载，并在 `setup()`（`main.cpp:183`）与 `loop()`（`main.cpp:226`）两处分流。模式切换采用 **「写入 NVS → `ESP.restart()` 重启」**（`main.cpp:144-146`、`main.cpp:156-158`），以重启换取彻底的状态复位——这是规避「引脚复用残留 / 中断未解绑」类问题的最直接手段。

### 2. 引脚分配总览（GPIO 复用架构）

模式切换键（GPIO 10）为唯一固定功能引脚，其余引脚在模式间时分复用。**注意：GPIO 0/1 现为触摸按键（active-HIGH），不再是编码器。**

| 物理引脚 | 公共通道 | 模式 1：飞鼠正常 | 模式 1：飞鼠断控 | 模式 2：电容触控 |
| :--- | :--- | :--- | :--- | :--- |
| **GPIO 0** | — | 触摸左键（鼠标左键） | **触摸「向上滚动」键** | 编码器 A 相 (TIM-CH1) |
| **GPIO 1** | — | 触摸右键（鼠标右键） | **触摸「向下滚动」键** | 编码器 B 相 (TIM-CH2) |
| **GPIO 2** | — | 断控切换键（进入/退出滚轮） | 断控切换键 | 触控芯片中断 (INT) |
| **GPIO 3** | — | 飞鼠灵敏度环形调节键 | 滚轮灵敏度环形调节键 | 触控芯片复位 (RST) |
| **GPIO 8** | I2C SDA @400kHz | MPU6050 / OLED | MPU6050 / OLED | FT6336U / OLED |
| **GPIO 9** | I2C SCL @400kHz | MPU6050 / OLED | MPU6050 / OLED | FT6336U / OLED |
| **GPIO 10** | 模式切换触摸键 | 短按→M2 / 长按→M3 | 短按→M2 / 长按→M3 | 短按→M1 / 长按→M3 |

**触摸按键电气约定**（`mouse_mode.cpp:37-38`）：触摸模块输出为**点动高电平**，故 `TOUCH_PRESSED = HIGH`、`TOUCH_RELEASED = LOW`，引脚统一配置为 `pinMode(pin, INPUT)`（`mouse_mode.cpp:258-261`、`main.cpp:171`）。

### 3. 参数体系与 NVS 持久化（v3 新增）

引入统一参数结构体 `SystemConfig`（`config.h`），把「档位」与「算法增益」解耦：

```cpp
struct SystemConfig {
    uint8_t air_dpi_level;      // 飞鼠灵敏度档位 (1~10)
    uint8_t touch_dpi_level;    // 触控/滚轮灵敏度档位 (1~10)
    uint8_t deadzone_px;        // 死区 (0~10)
    float   air_gain;           // 由档位映射的飞鼠算法增益
    float   touch_gain;         // 由档位映射的触控算法增益
};
```

**档位 → 增益映射**（`main.cpp:51-58`）：

| 目标 | 公式 | 取值范围 |
| :--- | :--- | :--- |
| 飞鼠增益 `air_gain` | `0.2 + air_dpi_level × 0.18` | 0.38 ~ 2.00 |
| 触控增益 `touch_gain` | `0.5 + touch_dpi_level × 0.25` | 0.75 ~ 3.00 |
| OLED 显示用百分比 | `global_sens_percent = air_dpi_level × 10`；`global_wheel_sens_percent = touch_dpi_level × 10` | 10 ~ 100 |

**NVS 持久化**（`main.cpp:60-83`，命名空间 `mouse_cfg`）：

| Key | 类型 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| `air_dpi` | UChar | 5 | 飞鼠档位 1~10 |
| `touch_dpi` | UChar | 5 | 触控/滚轮档位 1~10 |
| `deadzone` | UChar | 2 | 死区 0~10 |
| `sys_mode` | UChar | 0 | **当前模式也随之落盘**，开机恢复（越界值回落为 0，`main.cpp:78`） |

三个写入入口：App 下发参数（`main.cpp:103`）、模式切换（`main.cpp:144/156`）、按键调节灵敏度（`mouse_mode.cpp:223/240`、`touch_mode.cpp:182`）。

### 4. 代码分布与文件明细

代码划分为「主控调度」「硬件驱动」「手势/模式行为」三大层次：

* **`include/config.h`**（2128 B）
  引脚宏定义、模式枚举 `SystemMode`、参数结构体 `SystemConfig`、模式 3 的 `CONFIG_SERVICE_UUID` / `CONFIG_CHAR_UUID`、NVS 三个函数声明与 `extern` 全局声明。
* **`src/main.cpp`**（295 行）
  系统总入口与全局实体。负责：参数映射与 NVS 读写（`updateConfigGains` / `saveConfigToNVS` / `loadConfigFromNVS`）、**模式 3 的 GATT 服务与写回调**（`AppConfigCallbacks:88-110`、`ConfigServerCallbacks:112-118`）、**模式切换状态机**（`handleModeSwitch:123-162`）、`setup()` 三模式分支初始化（`167-217`）、`loop()` 时间片调度与三套 OLED 刷新（`222-292`）。
* **`src/mouse_mode.cpp`**（320 行）｜模式 1 行为层
  触摸按键状态机 `updateNormalButtons:43-157`（正常模式→左右键；断控模式→触摸控上下滚动 + 20ms 动力学累加连发）、断控切换 `updateLockKeyLogic:162-189`、灵敏度环形调节 `updateSensKeyLogic:194-244`、开机 50 次静态零偏标定与光标链路 `initMouseMode:249` / `updateMouseMode:277`。
* **`src/touch_mode.cpp`**（376 行）｜模式 2 行为层
  FT6336U 双点 I2C 解析 `readFT6336U_Dual:42-93`；EC11 中断捕获 `handleEncoderM2ISR:31`；手势状态机 `updateTouchMode:161`（单指移动 / 单击双击 / 拖拽 / 双指缩放 / 双指持续滚动 / 双指轻击右键 / 旋钮调参）。
* **`src/display_mode.cpp`**（211 行，本版未改动）｜OLED 渲染引擎
  模式 1、模式 2 两套界面 + `drawWheelConfigUI` 滚轮配置界面；依赖 `global_sens_percent` 等全局变量渲染。
* **`src/mpu6050_driver.cpp`**（100 行，本版未改动）｜MPU6050 寄存器级初始化与 14 字节原始数据流读取。
* **`src/imu_processing.c`**（48 行）｜姿态角解算接口，**仍为占位实现**（本版仅补充 `#include "mpu6050_driver.h"` 与 `#ifndef PI` 防御）。

### 5. 总体实现思路

核心思路仍为 **「时间片节流 + 状态机分流」**，但两处机制已完成升级：

* **主循环时间片**：HID 派发窗口 15ms（`main.cpp:241`）、OLED 刷新 100ms（`main.cpp:260`）、循环尾 `delay(6)` 主动让出 CPU 给 NimBLE 后台线程。连接建立后额外等待 2000ms 才派发数据（`main.cpp:245`），避免配对新链路时的抖动。
* **参数单一真源**：所有灵敏度/死区以 `g_cfg` 为唯一真源，`updateConfigGains()` 统一换算为算法增益并同步显示变量；App（模式 3）与板载按键（模式 1/2）改的是同一个结构体，且都会落盘。
* **断控滚动（v3 重写）**：由「EC11 寄存器直读 + 固定滚轮包」改为 **「触摸点击 + 动力学累加器」**——触摸瞬间发 1 格首包（零延迟触感），按住后以 20ms 为周期按灵敏度累加连发，松手立即清零刹车（`mouse_mode.cpp:90-155`）。速度公式：`speed = 0.05 + (wheel_sens% / 100) × 0.80`（`mouse_mode.cpp:132`）。
* **触控持续滚动（沿用）**：位置/速度时钟触发，手指越过 20px 判定线且未抬起时，由内部定时器每 40ms 持续补发滚轮包，从机制上消除断续卡顿。

---

## ⚠️ 二、现有代码缺失及潜在缺陷检查

按严重度排序。**A 类为功能/寿命风险，建议优先处理。**

### A. 高优先级

1. **NVS 高频写入，存在 Flash 磨损风险**
   长按灵敏度调节时，每 150ms 就调用一次 `saveConfigToNVS()`（`mouse_mode.cpp:210-223`）；模式 2 旋钮每步进一次也落盘（`touch_mode.cpp:181-182`）。ESP32 的 NVS 分区擦写寿命约 10 万次量级，按 150ms/次估算，**连续长按约 4 小时即可耗尽一个 NVS 页的擦写次数**。
   **建议**：改为「脏标记 + 松手/超时批量落盘」（例如松手后 1~2s 写一次），或至少加 10~30s 的落盘节流。

2. **`is_ctrl_pressed` 是死逻辑：模式 2 的「旋钮发滚轮」分支不可达**
   全项目检索确认，该变量**只有初始化、没有任何赋值点**（`main.cpp:33` 定义为 `false`，`touch_mode.cpp:29` extern，`touch_mode.cpp:172` 判断）。因此 `touch_mode.cpp:172-175` 的滚轮分流**永远不会执行**，旋钮只能调灵敏度，无法滚动页面。
   **建议**：要么补上触发源（例如与模式 1 断控按键联动），要么删除该分支与变量，避免误导后续维护。

3. **模式 2 的 EC11 中断仍缺少软件防抖**
   `handleEncoderM2ISR`（`touch_mode.cpp:31-34`）仍是「读 A/B 相 → `aState == bState` 判向 + 计数」的简化实现，仅靠输出侧 120ms 去抖（`KNOB_DEBOUNCE_MS`）压制抖动。硬件无 RC 滤波时，慢速转动或停在临界点仍可能出现**反向误计数**（模式 1 原有的寄存器直读方案已随触摸改写而移除，这套防抖经验未迁移过来）。
   **建议**：引入 4 位正交格雷码状态机（可参考 v2 中 `REG_READ(GPIO_IN_REG)` 的写法）或二级判定。

### B. 中优先级

4. **`deadzone_px` 一个参数承担两种物理单位**
   输入侧死区为 `deadzone_px × 40`（原始陀螺仪 LSB 单位，`mouse_mode.cpp:296`），输出侧死区为 `deadzone_px`（像素单位，`mouse_mode.cpp:309-310`）。二者随同一滑杆联动但语义不同：在 ±500°/s 量程（65.5 LSB/(°/s)）下，`deadzone_px = 10` 相当于 **约 6.1 °/s 的输入死区**，足以吞掉慢速移动。
   **建议**：拆成「输入死区（LSB）」与「输出死区（px）」两个参数，或明确限定档位上限。

5. **触摸引脚使用 `INPUT` 且无上下拉，悬空即随机**
   `mouse_mode.cpp:258-261` 与 `main.cpp:171` 均配置为 `INPUT`（适配触摸模块推挽高电平）。若触摸模块未接线或接触不良，引脚悬空会使读数随机跳变，可能造成**误触发按键/模式切换**。v2 使用 `INPUT_PULLUP` 时不存在该风险。
   **建议**：确认触摸模块输出为推挽后，保留现状但在文档中标注；或加入上电自检（读一次引脚，异常则提示）。

6. **模式 3 与 HID 互斥，且 App 写参无回读**
   模式 3 只注册自定义 GATT 服务（`main.cpp:187-203`），不启动 `BleMouse`，因此**进入调参模式后电脑鼠标即失联**，无法「边调边试」。同时该特征值仅开放 `READ/WRITE/WRITE_NR`，**没有 NOTIFY**，App 无法回读设备当前参数，OLED 是唯一反馈通道（`main.cpp:269-287`）。
   **建议**：① 评估「HID + 配置服务共存」方案（`CONFIG_BT_NIMBLE_MAX_CONNECTIONS=2` 已预留并发能力）；② 为特征值增加 NOTIFY，参数变更后主动上报，App 即可显示真实值。

7. **`CONFIG_BT_NIMBLE_MAX_CONNECTIONS=2` 与当前实现不匹配**
   `platformio.ini` 已放开双并发连接（注释写明「1 个给电脑鼠标，1 个给手机 App 调参」），但当前模式 3 下并未注册 HID 服务，双连接能力实际无使用场景，属于**提前预留但未落地**的配置。
   **建议**：随第 6 项一并落地，或暂时改回 1 以进一步压缩内存占用。

8. **模式切换依赖 `ESP.restart()`，切换成本较高**
   长短按均以「落盘 + 重启」收尾（`main.cpp:145-146`、`157-158`），切换过程约 1s 内设备不可用、BLE 需重新连接；且长按需**松手后**才判定（`pressDuration` 在释放瞬间计算，`main.cpp:133-136`），手感上会多等一次抬手。
   **建议**：长按达到阈值即刻触发（不必等松手）；中长期以「软切换 + 状态复位」替代重启（见规划第 4 节）。

### C. 清理项（不影响功能，但影响可读性）

9. **死代码残留**：`sensitivityList`（`main.cpp:28`）与 `currentSensIdx`（`main.cpp:29`）在本版已无任何使用点，仅剩 `touch_mode.cpp:9-10` 的 `extern` 声明，建议连同删除。
10. **注释与实现不符**：`display_mode.cpp:26` 仍写着「实时获取 EC11 滚轮编码器物理计数」，但 `mouseWheelCount` 现在是由触摸滚动逻辑累加、仅供 OLED 动画使用的计数器（`mouse_mode.cpp:98/115/141/152`）。
11. **速度注释数值偏差**：`mouse_mode.cpp:129-131` 注释与当前公式不符——注释写「50% → 速度 0.35、约 57ms/格」，而按 `0.05 + 0.5 × 0.80 = 0.45`、20ms/周期计算，实际约 **0.45 / 44ms 一格**（10% 档注释 0.12，实际 0.13；100% 档 0.85 与实际一致）。注释为旧公式残留，建议按现公式重算。
12. **`imu_processing.c` 仍为占位**：互补滤波/欧拉角解算未接入主链路，光标目前完全依赖陀螺仪原始值积分与死区处理（模式 1 只用 `data_ptr[5]/[6]` 两个陀螺仪轴）。
13. **显示层未接入参数体系**：`display_mode.cpp` 仍 `extern` 旧的 `global_sens_percent` / `global_wheel_sens_percent`，靠 `updateConfigGains()` 反向同步（`main.cpp:56-57`）间接渲染，属于隐性耦合，建议改为直接接收 `g_cfg` 或显式传参。

---

## 🔮 三、未来优化计划与发展建议

### 1. 触控屏手势

* **动态锚点中心缩放**：当前双指捏合（`distDelta`）固定发送中键+滚轮信号。可计算双指几何中心 $(X_c, Y_c)$，按中心位于屏幕左/右侧动态调整缩放包频率，使其更贴近 CAD / EDA 软件的直觉。
* **右键二次判定加固**：双指轻击右键目前仅靠 `<300ms` 时间差（`touch_mode.cpp:353`）。建议引入双指间距突变率过滤——若抬起瞬间两点距离大幅发散，说明是滑动的收尾动作，应剔除。
* **双指滚动速度阶梯**：持续滚动目前恒定 20px 触发 + 40ms 周期，可按双指位移量分级（小幅=精确滚动，大幅=快速翻页）。

### 2. 飞鼠与滚轮

* **虚拟飞轮（Flywheel）**：断控滚轮已实现「按住连发」，可继续叠加**惯性滑行**——检测到快速连击后，在松手后以指数衰减频率补发几帧滚轮包，模拟无阻尼滚轮手感。
* **单击/双击语义扩展**：GPIO 0/1 现为触摸按键，具备识别单击/双击的硬件基础，可扩展为「单击=上下滚一格、双击=翻页/中键、长按=连续滚动」，进一步减少对模式切换的依赖。
* **长按加速曲线可调**：当前连发速度为线性映射（`0.05 + sens% × 0.80`），可改为曲线映射或按「已按住时长」递增，兼顾近距精调与长文档快速浏览。

### 3. 参数系统与 App 联动

* **参数回读（NOTIFY）**：让 App 显示设备真实参数，替代「盲写 + 看 OLED」。
* **落盘节流与脏标记**：解决 A-1 的 Flash 磨损（详见缺陷 1）。
* **参数分组扩展**：在 `SystemConfig` 中增加滤镜系数（α）、DAMPING、连发周期等，使 App 成为完整的调试终端。
* **调参实时预览**：模式 3 下让 OLED 显示「参数变更历史/最近一次写入」，便于现场确认。

### 4. 系统级重构

* **统一全局事件总线（Event Bus）**：把长按/短按/双击等按键事件抽象为独立事件，取代各模块内互相 `digitalRead` + 就地分流的写法（当前 `updateSensKeyLogic` 仍在内部用 `is_airmouse_locked` 切换语义），从根本上消除断控模式下按键语义混用一类问题。
* **模式软切换（免重启）**：以「反初始化 + 重新初始化」的显式生命周期函数替代 `ESP.restart()`，保留 BLE 连接、缩短切换时间。
* **统一传感器抽象层**：为 IMU / 触控芯片定义统一接口，支持器件替换与单元测试（当前 `imu_processing.c` 的占位状态应随之补齐或删除）。
* **代码清理**：按 C 类清单清理死代码与过时注释，保持文档与实现一致。

---

## 📎 附录：关键阈值速查表

| 类别 | 参数 | 值 | 位置 |
| :--- | :--- | :--- | :--- |
| 调度 | HID 派发窗口 | 15ms | `main.cpp:241` |
| 调度 | OLED 刷新 | 100ms | `main.cpp:260` |
| 调度 | 主循环让出 | delay(6) | `main.cpp:291` |
| 调度 | 连接后启动延迟 | 2000ms | `main.cpp:245` |
| 飞鼠 | 光标链路节流 | 12ms | `mouse_mode.cpp:286` |
| 飞鼠 | 低通滤波系数 α | 0.4（一阶 EMA） | `mouse_mode.cpp:26` |
| 飞鼠 | 位移阻尼 DAMPING | 120 | `mouse_mode.cpp:23` |
| 飞鼠 | 输入死区 | `deadzone_px × 40` LSB | `mouse_mode.cpp:296` |
| 飞鼠 | 输出死区 | `deadzone_px` px（末级归零） | `mouse_mode.cpp:309` |
| 飞鼠 | 开机零偏标定 | 50 次采样平均 | `mouse_mode.cpp:266` |
| 按键 | 左右键去抖 | 20ms | `mouse_mode.cpp:62/75` |
| 按键 | 断控切换去抖 | 50ms | `mouse_mode.cpp:168` |
| 按键 | 灵敏度长按触发 / 自动步进 | 400ms / 150ms | `mouse_mode.cpp:210/212` |
| 按键 | 模式键短按 / 长按 | ≥50ms / ≥1500ms | `main.cpp:148/136` |
| 滚轮 | 断控连发 tick | 20ms | `mouse_mode.cpp:125` |
| 滚轮 | 连发速度公式 | `0.05 + sens% × 0.80` | `mouse_mode.cpp:132` |
| 触控 | 滑动判定 / 缩放判定 | 10px / 6px | `touch_mode.cpp:120/122` |
| 触控 | 单击窗口 / 双指右键窗口 | 250ms / <300ms | `touch_mode.cpp:121/353` |
| 触控 | 双指冷却 / 旋钮去抖 | 150ms / 120ms | `touch_mode.cpp:123/124` |
| 触控 | 持续滚动触发 / 周期 | 20px / 40ms | `touch_mode.cpp:126/127` |
| 参数 | 飞鼠档位→增益 | `0.2 + level × 0.18` | `main.cpp:52` |
| 参数 | 触控档位→增益 | `0.5 + level × 0.25` | `main.cpp:53` |
| 参数 | NVS 命名空间 | `mouse_cfg` | `main.cpp:61` |
