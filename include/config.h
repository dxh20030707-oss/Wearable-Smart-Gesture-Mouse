#ifndef CONFIG_H
#define CONFIG_H

// ==========================================
// 1. 公共 I2C 总线引脚 (MPU6050 + 触控芯片 + OLED 共享)
// ==========================================
#define PIN_I2C_SDA        8
#define PIN_I2C_SCL        9

// ==========================================
// 2. 复用引脚定义 (GPIO 0, 1, 2, 3) 
// ==========================================

// 🟢 【模式 1 功能物理连线对齐】
#define PIN_M1_LEFT        0  // 物理左键 (连在 GPIO 0)
#define PIN_M1_RIGHT       1  // 物理右键 (连在 GPIO 1)
#define PIN_M1_PAUSE       2  // 按下时断开/恢复飞鼠控制 (连在 GPIO 2)
#define PIN_M1_SENS        3  // 飞鼠/滚轮灵敏度环形切换按键 (连在 GPIO 3)

// 🔵 【模式 2 功能物理连线对齐 - 🎯 已彻底根据您的要求纠正】
#define PIN_M2_TIM_CH1     0  // 旋钮正交编码器 A 相 (CLK) -> 连在 GPIO 0
#define PIN_M2_TIM_CH2     1  // 羊纽正交编码器 B 相 (DT)  -> 连在 GPIO 1
#define PIN_M2_INT         2  // 触控芯片中断引脚 (INT)     -> 连在 GPIO 2
#define PIN_M2_RST         3  // 触控芯片复位引脚 (RST)     -> 连在 GPIO 3

// ==========================================
// 3. 核心模式切换物理按键
// ==========================================
#define PIN_MODE_SWITCH    10 // 按下在 飞鼠/触控屏 之间切换

// ==========================================
// 🎯 全局共享变量声明 (防多重定义冲突)
// ==========================================
// 告诉编译器此变量在 main.cpp 中定义，其他地方包含本头文件即可无缝借调
extern bool is_ctrl_pressed;

#endif // CONFIG_H